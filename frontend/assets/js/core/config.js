// /assets/js/core/config.js

export const CONFIG = {
  // Backend (Express) serves the frontend + API from the SAME origin.
  // So relative base works both locally and after deployment.
  API_BASE: "/api",
  TOKEN_KEY: "roboticsleb_token",
  ADMIN_TOKEN_KEY: "roboticsleb_admin_token"
};
