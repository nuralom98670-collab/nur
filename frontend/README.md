# RoboticsLeb — Fullstack (Admin + Shop + Blog)

## Run

```bash
npm install
npm run dev
```

Open:

- Home: http://localhost:5000/
- Shop: http://localhost:5000/shop.html
- Blog: http://localhost:5000/blog.html
- Admin login: http://localhost:5000/admin/login.html

## Login

This build uses **dev login** (because real auth UI comes later). Any email/password will work.

## Database

Data is stored in **SQLite**:

- `server/data/app.db`

Tables are auto-created on first run.
